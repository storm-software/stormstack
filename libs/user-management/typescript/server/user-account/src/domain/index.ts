import { userManagementTypescriptServerUserManagementServerUserAccount } from "./user-account-aggregate";

describe("userManagementTypescriptServerUserManagementServerUserAccount", () => {
  it("should work", () => {
    expect(
      userManagementTypescriptServerUserManagementServerUserAccount()
    ).toEqual(
      "user-management-typescript-server-user-management-server-user-account"
    );
  });
});
