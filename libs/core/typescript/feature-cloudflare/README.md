# core-feature-cloudflare

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build core-feature-cloudflare` to build the library.

## Running unit tests

Run `nx test core-feature-cloudflare` to execute the unit tests via [Jest](https://jestjs.io).
