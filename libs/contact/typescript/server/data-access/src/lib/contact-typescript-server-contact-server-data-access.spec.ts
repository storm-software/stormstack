import { contactTypescriptServerContactServerDataAccess } from "./contact-typescript-server-contact-server-data-access";

describe("contactTypescriptServerContactServerDataAccess", () => {
  it("should work", () => {
    expect(contactTypescriptServerContactServerDataAccess()).toEqual(
      "contact-typescript-server-contact-server-data-access"
    );
  });
});
