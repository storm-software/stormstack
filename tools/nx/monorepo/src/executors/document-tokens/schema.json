{
  "$schema": "http://json-schema.org/schema",
  "cli": "nx",
  "id": "document-tokens",
  "title": "StormStack - Nx-Monorepo Design Tokens Documenter",
  "description": "📱 Create documentation for the monorepo's design tokens",
  "type": "object",
  "properties": {
    "tokensPath": {
      "type": "string",
      "description": "The path to the design tokens code"
    },
    "outputPath": {
      "type": "string",
      "description": "The path to generate the design tokens documentation in"
    }
  },
  "required": ["tokensPath", "outputPath"]
}
