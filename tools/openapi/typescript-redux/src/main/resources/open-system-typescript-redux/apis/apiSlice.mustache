import { fetchHttpHandler } from "@open-system/core-typescript-utilities";
import { createApi } from "@reduxjs/toolkit/query/react";

{{#apiInfo}}
{{#apis}}
// initialize an empty api service that we'll inject endpoints into later as needed
export const {{#lambda.camelcase}}{{domainName}}{{/lambda.camelcase}}ApiSlice = createApi({
  reducerPath: "{{#lambda.camelcase}}{{domainName}}{{/lambda.camelcase}}Api",
  baseQuery: fetchHttpHandler({ baseUrl: `${ process.env.NEXT_PUBLIC_{{#lambda.uppercase}}{{domainName}}{{/lambda.uppercase}}_API_HOST }/api/v{{{version}}}{{^version}}1{{/version}}` }),
  endpoints: () => ({}),
});
{{/apis}}
{{/apiInfo}}