{
  "$schema": "http://json-schema.org/schema",
  "cli": "nx",
  "title": "Open System - Client Redux Application Sync",
  "description": "Sync up TypeScript client-side API SDK project code and types based on an OpenApi spec",
  "type": "object",
  "properties": {
    "outputFileTypes": {
      "type": "string",
      "alias": "output",
      "description": "The path (relative to the workspace root) where the OpenAPI JSON spec file is located",
    },
    "outputFileSchemaAst": {
      "type": "string",
      "alias": "output",
      "description": "The path (relative to the workspace root) where the OpenAPI JSON spec file is located",
    },
    "outputFileClient": {
      "type": "string",
      "alias": "output",
      "description": "The path (relative to the workspace root) where the OpenAPI JSON spec file is located",
    }
  },
  "required": ["outputFileTypes", "outputFileSchemaAst", "outputFileClient"]
}
