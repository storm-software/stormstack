{
  "$schema": "http://json-schema.org/schema",
  "cli": "nx",
  "id": "Restore",
  "title": ".Net Restore Generator",
  "type": "object",
  "description": "Restores NuGet packages and .NET tools used by the workspace.",
  "properties": {
    "configFile": {
      "type": "string",
      "description": "The path to the NuGet.Config file to use during the restore"
    }
  }
}
