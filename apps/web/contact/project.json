{
  "$schema": "../../../node_modules/nx/schemas/project-schema.json",
  "name": "web-contact",
  "projectType": "application",
  "sourceRoot": "apps/web/contact",
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "outputs": ["{workspaceRoot}/dist/apps/web/contact"],
      "defaultConfiguration": "development",
      "options": {
        "parallel": true
      },
      "configurations": {
        "development": {
          "commands": [
            "nx run web-contact:build-base:development",
            "cd apps/web/contact/ClientApp && npm run build"
          ]
        },
        "production": {
          "commands": [
            "nx run web-contact:build-base:production",
            "cd apps/web/contact/ClientApp && npm run build"
          ]
        }
      }
    },
    "build-base": {
      "executor": "@nx-dotnet/core:build",
      "outputs": ["{workspaceRoot}/dist/apps/web/contact"],
      "defaultConfiguration": "development",
      "options": {
        "configuration": "Debug",
        "noDependencies": true
      },
      "configurations": {
        "development": {
          "configuration": "Debug",
          "noDependencies": true
        },
        "production": {
          "configuration": "Release"
        }
      }
    },
    "serve": {
      "executor": "nx:run-commands",
      "defaultConfiguration": "development",
      "options": {
        "parallel": true
      },
      "configurations": {
        "development": {
          "commands": [
            "nx run web-contact:serve-base:development",
            "cd apps/web/contact/ClientApp && npm run dev"
          ]
        },
        "production": {
          "commands": [
            "nx run web-contact:serve-base:production",
            "cd apps/web/contact/ClientApp && npm run dev"
          ]
        }
      }
    },
    "serve-base": {
      "executor": "@nx-dotnet/core:serve",
      "options": {
        "configuration": "Debug",
        "defaultConfiguration": "development"
      },
      "configurations": {
        "development": {
          "configuration": "Debug"
        },
        "production": {
          "configuration": "Release"
        }
      }
    },
    "publish": {
      "executor": "@nx-dotnet/core:publish",
      "selfContained": true,
      "outputs": ["dist/apps/web/contact/publish"],
      "options": {
        "configuration": "Debug",
        "noDependencies": true
      },
      "configurations": {
        "production": {
          "configuration": "Release"
        },
        "docker": {
          "configuration": "Debug",
          "outputs": "/app/publish"
        }
      }
    },
    "lint": {
      "executor": "@nx-dotnet/core:format"
    },
    "test": {
      "executor": "@nrwl/jest:jest",
      "outputs": ["{workspaceRoot}/coverage/{projectRoot}"],
      "options": {
        "jestConfig": "apps/web/contact/ClientApp/jest.config.ts",
        "passWithNoTests": true
      }
    },
    "semantic-release": {
      "executor": "@theunderscorer/nx-semantic-release:semantic-release",
      "options": {
        "github": true,
        "npm": false,
        "changelog": true,
        "tagFormat": "web-contact-v${VERSION}"
      }
    }
  },
  "tags": ["domain:shared", "type:apps", "target:web"]
}

