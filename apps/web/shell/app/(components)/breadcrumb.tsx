"use client";

import { Breadcrumb as BreadcrumbInner, BreadcrumbProps } from "@open-system/shared-ui-components/breadcrumb";

export default function Breadcrumb(props: BreadcrumbProps) {
  return <BreadcrumbInner {...props} />;
}
